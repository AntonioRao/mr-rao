# Mr. Rao — runtime dependencies (offline after first install / portable build)
flask>=3.0.3
werkzeug>=3.0.3
markitdown>=0.1.7
# I formati Office di MarkItDown vivono dietro degli "extra", e senza di
# essi DOCX, XLSX, XLS e PPTX non si convertono affatto: MarkItDown alza
# MissingDependencyException e il documento sembra vuoto. Erano dichiarati
# nella pagina, nei badge e nel menu contestuale, e non hanno mai
# funzionato. I pacchetti sono elencati per nome, non come extra.
#
# ATTENZIONE, lezione pagata: questi nomi vanno letti dai sorgenti di
# MarkItDown, non indovinati dall'estensione. Il primo tentativo aveva messo
# python-docx, che sembra ovvio ed e' sbagliato: per il .docx MarkItDown
# importa **mammoth**. Qui in locale non se n'e' accorto nessuno perche' il
# venv aveva gia' mammoth da un'installazione precedente -- l'ha trovato la
# CI, che parte pulita. Se cambi questa lista, guarda cosa importano davvero
# i converter in markitdown/converters/.
mammoth>=1.6.0          # .docx  -> _docx_converter.py
python-pptx>=0.6.23     # .pptx  -> _pptx_converter.py (import pptx)
pandas>=2.0.0           # .xlsx e .xls -> _xlsx_converter.py, insieme a:
openpyxl>=3.1.0         # .xlsx
xlrd>=2.0.1             # .xls
pdfminer.six>=20231228
pdfplumber>=0.11.0
Pillow>=10.0.0
# OCR offline. `rapidocr` e non piu' `rapidocr_onnxruntime`: il pacchetto e'
# stato rinominato e il vecchio nome e' fermo alla 1.2.3, senza piu' rilasci
# nemmeno di sicurezza. `onnxruntime` va dichiarato a parte, perche' dalla 3.x
# non e' piu' una dipendenza di rapidocr: senza, il primo RapidOCR() alza
# ImportError. I modelli PP-OCRv6 viaggiano dentro la wheel.
rapidocr>=3.9.2
onnxruntime>=1.17.0
# Esportazione in .docx: i documenti che devono restare documenti (atti da
# pubblicare, contratti da depositare) non possono uscire in Markdown.
# MIT, dipende solo da lxml (BSD-3-Clause): nessun copyleft nuovo.
python-docx>=1.1.0
# HTML → text for .eml — see docs/BEAUTIFULSOUP.md
beautifulsoup4>=4.12.0
# System tray (optional but recommended)
pystray>=0.19.5
# Tests
pytest>=8.0.0
# usato dai test per verificare che il frontmatter sia YAML valido
PyYAML>=6.0
