[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers =
    slow: alza un processo vero (secondi, non millisecondi). Restano nella
        suite normale: sono proprio quelli che provano il giro completo, e
        escluderli per abitudine significa non provarlo piu'.
