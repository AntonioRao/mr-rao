services:
  mr-rao:
    build: .
    ports:
      - "5000:5000"
    environment:
      MR_RAO_HOST: "0.0.0.0"
      MR_RAO_PORT: "5000"
      MR_RAO_DEBUG: "0"
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
